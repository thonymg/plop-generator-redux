
  it('should select get{{ properCase selectorName }}', () => {
    const stateBefore = null
    const stateAfter = null

    expect(
      selectors.get{{ properCase selectorName }}({ {{> reducerName }}: stateBefore })
    ).toEqual({ {{> reducerName }}: stateAfter })
  })
$1