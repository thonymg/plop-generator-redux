
  it('should create the {{ camelCase actionName }} action', () => {
    expect(
      actions.{{ camelCase actionName }}()
    ).toEqual({
      type: actions.{{ constantCase actionName }},
      payload: {}
    })
  })
$1